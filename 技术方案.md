# 打卡精灵 — 技术方案

## 1. 技术选型

### 1.1 总体架构

```
┌─────────────────────────────────────────┐
│            PWA Shell                     │
│  (manifest.json + service worker)        │
├─────────────────────────────────────────┤
│          Vue 3 + Vite (SPA)             │
├──────────────┬──────────────────────────┤
│  儿童模式     │       家长管理模式         │
│  (语音交互)   │       (任务配置)          │
├──────────────┴──────────────────────────┤
│        状态管理: Pinia                    │
│        持久化: IndexedDB (idb)           │
│        语音: Web Speech API              │
│        样式: UnoCSS                      │
└─────────────────────────────────────────┘
```

### 1.2 核心依赖

| 技术 | 选型 | 原因 |
|------|------|------|
| 框架 | Vue 3 + Composition API | 轻量、响应式 |
| 构建 | Vite | 快速 HMR、PWA 插件 |
| 状态管理 | Pinia | 简洁 |
| 路由 | vue-router | 双模式切换 |
| 存储 | idb (IndexedDB) | 结构化存储 |
| 样式 | UnoCSS | 按需生成 |
| PWA | vite-plugin-pwa | 自动生成 SW |
| 语音 | Web Speech API | 浏览器原生、免费 |

---

## 2. 目录结构

```
daka/
├── index.html
├── package.json
├── vite.config.ts
├── uno.config.ts
├── .github/workflows/
│   └── deploy.yml
├── tsconfig.json
├── public/icons/
│   ├── icon-192.png
│   └── icon-512.png
├── src/
│   ├── main.ts
│   ├── App.vue
│   ├── types/index.ts
│   ├── db/index.ts
│   ├── router/index.ts
│   ├── stores/
│   │   ├── tasks.ts
│   │   ├── records.ts
│   │   ├── voice.ts
│   │   └── settings.ts
│   ├── composables/
│   │   ├── useSpeechRecognition.ts
│   │   ├── useSpeechSynthesis.ts
│   │   └── useVoiceCommands.ts
│   ├── views/
│   │   ├── ChildView.vue
│   │   └── ParentView.vue
│   └── components/
│       ├── child/
│       │   ├── CharacterAvatar.vue
│       │   ├── TaskList.vue
│       │   ├── VoiceButton.vue
│       │   ├── VoiceDialog.vue
│       │   ├── PointsDisplay.vue
│       │   └── CelebrationEffect.vue
│       └── parent/
│           ├── TaskForm.vue
│           ├── TaskItem.vue
│           └── PointsHistory.vue
```

---

## 3. 数据流设计

### 3.1 整体数据流

```
IndexedDB (持久层) ←→ Pinia Store (状态层) ←→ Vue 视图 + 语音引擎
```

### 3.2 语音交互数据流

```
小朋友说话 → SpeechRecognition → useVoiceCommands 解析
  → 匹配命令 → 操作 Store → 生成回复文本 → SpeechSynthesis → 小朋友听到
```

### 3.3 命令匹配策略

采用关键词模糊匹配：
- "背古诗已完成" → 匹配任务名"背古诗" + 关键词"已完成"
- 支持变体：做完了、完成了、搞定、打卡

---

## 4. 数据库设计

### 4.1 数据库: `daka精灵`

| Object Store | Key | 索引 |
|-------------|-----|------|
| `tasks` | id (autoIncrement) | sortOrder |
| `records` | id (autoIncrement) | date, taskId, dateTaskId |
| `settings` | key (string) | — |

---

## 5. 语音模块

### 5.1 useSpeechRecognition

- 封装 Web Speech API
- lang: zh-CN, continuous: true, interimResults: true

### 5.2 useSpeechSynthesis

- rate: 0.9（适合儿童）, pitch: 1.2

### 5.3 useVoiceCommands

- parse(text) → VoiceCommand
- execute(command) → reply text

---

## 6. 路由设计

| 路径 | 视图 | 说明 |
|------|------|------|
| `/` | ChildView | 儿童主界面（默认） |
| `/parent` | ParentView | 家长管理（需密码） |

---

## 7. PWA 配置

- Manifest: standalone, portrait, 主题色 #FFB347
- Service Worker: 预缓存静态资源，离线可用
- iPad 适配: viewport 禁用缩放、apple-mobile-web-app-capable

---

## 8. 状态管理 (Pinia)

| Store | 职责 |
|-------|------|
| tasks | 任务 CRUD、获取今日任务 |
| records | 打卡记录、今日/总积分计算 |
| voice | 语音状态、对话历史 |
| settings | 孩子称呼、语音速度、家长密码 |

---

## 9. 样式方案

- 主题色: 暖橙 #FFB347、天蓝 #4FC3F7、翠绿 #66BB6A
- 米白背景 #FFF8E7
- 大圆角、柔和阴影
- 庆祝动画 CSS 粒子效果

---

## 10. 部署方案对比与选择

### 10.1 总体原则

所有打卡数据存储在 iPad 本地 IndexedDB 中，部署平台**只托管前端静态页面文件**，不接触任何用户数据。切换部署方式不会丢失打卡数据。

### 10.2 部署方式对比

| 特性 | Vercel | Surge.sh | Cloudflare Pages | Coding Pages | Gitee Pages | **GitHub Pages** |
|------|--------|----------|-----------------|-------------|-------------|-----------------|
| **费用** | 免费 | 免费 | 免费 | 免费（6个月流量包） | 免费 | **免费** |
| **HTTPS** | ✅ 自动 | ✅ 自动 | ✅ 自动 | ✅ 自动 | ✅ 自动 | **✅ 自动** |
| **部署方式** | `vercel dist --prod` | `surge dist` | `wrangler pages deploy dist` | Git 推送 + CI/CD | Git 推送 | **Git 推送 + GitHub Actions** |
| **配置复杂度** | ⭐ 低 | ⭐ 最低 | ⭐⭐ 中 | ⭐⭐⭐ 较高 | ⭐⭐ 中 | **⭐⭐ 中** |
| **国内访问** | ❌ 被屏蔽 | ❌ 不稳定 | ⚠️ 免费版不稳定 | ✅ 国内快 | ✅ 国内快 | **⚠️ 有时快有时慢** |
| **实名认证** | ❌ 不需要 | ❌ 不需要 | ❌ 不需要 | ✅ 需要 | ✅ 需要 | **❌ 不需要** |
| **ICP备案** | ❌ 不需要 | ❌ 不需要 | ❌ 不需要 | ⚠️ 自定义域名需要 | ⚠️ 自定义域名需要 | **❌ 不需要** |
| **一行命令部署** | ✅ 支持 | ✅ 支持 | ✅ 支持 | ❌ 需 Git 推送 | ❌ 需 Git 推送 | **❌ 需 Git 推送** |
| **PWA 支持** | ✅ 完整 | ✅ 完整 | ✅ 完整 | ✅ 完整 | ✅ 完整 | **✅ 完整** |
| **注册要求** | GitHub/邮箱 | 邮箱 | GitHub/邮箱 | 腾讯云账号 | Gitee 账号 | **GitHub 账号** |
| **公司背景** | 美国 | 荷兰 | 美国 | 腾讯云 | 开源中国 | **微软（美国）** |

### 10.3 各方案详细分析

#### Vercel（尝试过，国内不可用）

- 部署极简，体验最佳
- ❌ **国内网络无法访问 `.vercel.app` 域名**
- 已部署的 `https://daka-jingling.vercel.app` 国内打不开，放弃

---

#### Surge.sh

- 部署最简单：`surge dist xxx.surge.sh`
- 无需注册 GitHub，仅需邮箱
- ❌ **国内访问不稳定**，经常无法访问或速度慢
- 结论：放弃

---

#### Cloudflare Pages

- 全球 CDN 节点最多（330+）
- 免费版无限带宽
- Wrangler CLI 部署：`npx wrangler pages deploy dist`
- ⚠️ **免费版没有国内 CDN 节点**，`*.pages.dev` 在国内同样可能受限
- 绑定自定义域名且备案后才稳定
- 结论：可作为备选，但无国内节点仍需碰运气

---

#### Coding Pages（腾讯云）

| 项目 | 说明 |
|------|------|
| 公司 | **腾讯云**旗下，国内访问最快、最稳定 |
| 费用 | 免费开通，赠送 6 个月流量包，之后按量计费（用量很小则几元/月） |
| HTTPS | 自动免费 SSL 证书（TrustAsia） |
| 部署方式 | Git 推送代码 → CI/CD 自动构建部署 |
| 自定义域名 | ✅ 支持（需 ICP 备案） |
| 实名认证 | ✅ **需要**（国内云平台要求） |

**部署步骤**：
1. 注册 [腾讯云 CODING](https://cloud.tencent.com/product/coding) 并完成实名认证
2. 创建 DevOps 项目和代码仓库
3. 本地推送代码到仓库
4. 在「持续部署 → 静态网站」中开启 Pages 服务
5. 获得访问地址（格式：`https://coding-pages-bucket-xxx.cos-website.ap-guangzhou.myqcloud.com`）

**优点**：
- ✅ 国内访问有保障（腾讯云 CDN）
- ✅ 自动 HTTPS
- ✅ 支持 SPA 路由配置

**缺点**：
- ❌ 需要实名认证（身份证）
- ❌ 没有一行命令部署的方式，必须走 Git 推送
- ❌ 免费流量包有 6 个月期限，到期后按量计费
- ❌ 默认访问地址又长又难看（长串 COS 链接）

---

#### Gitee Pages（码云）

| 项目 | 说明 |
|------|------|
| 公司 | **开源中国（OSChina）**，国内平台 |
| 费用 | 免费 |
| HTTPS | 支持 |
| 部署方式 | Git 推送代码到 Gitee 仓库，开启 Pages 服务 |
| 自定义域名 | ⚠️ 仅 Gitee Pro 付费版支持自定义域名 |
| 实名认证 | ✅ 需要 |

**优点**：
- ✅ 国内访问速度快
- ✅ 免费 HTTPS
- ✅ 操作简单，类似 GitHub Pages

**缺点**：
- ❌ 免费版不支持自定义域名（只能用 `xxx.gitee.io`）
- ❌ Gitee Pages 服务已于 2024 年 5 月关闭下线
- ❌ 实名认证需要手机号

---

#### GitHub Pages（当前选用方案）

| 项目 | 说明 |
|------|------|
| 公司 | **GitHub（微软）**，全球最大代码托管平台 |
| 费用 | 免费（公开仓库无限） |
| HTTPS | 自动免费（\*.github.io） |
| 部署方式 | GitHub Actions CI/CD 自动构建部署 |
| 自定义域名 | ✅ 支持（需配置 CNAME） |
| 实名认证 | ❌ 不需要 |
| 国内访问 | ⚠️ 有时快有时慢，取决于网络环境 |

**优点**：
- ✅ 完全免费，无需实名认证
- ✅ 自动 HTTPS
- ✅ GitHub Actions CI/CD 自动化部署
- ✅ 全球 CDN（Fastly），国内多数地区可访问

**缺点**：
- ❌ 国内没有专用 CDN 节点，高峰期可能慢
- ❌ GitHub 本身在国内偶尔被屏蔽
- ❌ 不能一行命令部署，需走 Git 推送 + CI

**部署配置**:
```yaml
# .github/workflows/deploy.yml
name: 部署到 GitHub Pages
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - run: npm run build
      - uses: actions/configure-pages@v4
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist
      - id: deployment
        uses: actions/deploy-pages@v4
```

**Vite 配置要点**:
- `base: '/daka-jingling/'` — 子目录部署，对应仓库名
- 路由使用 `createWebHashHistory()` — 避免 404（GitHub Pages 不支持 SPA fallback）

---

### 10.4 国内访问方案总结

所有海外平台（Vercel、Surge、Cloudflare Pages 免费版）的共同问题：**没有国内 CDN 节点**，能否访问取决于网络环境，没有保障。

国内平台（Coding Pages、Gitee Pages）的共同问题：**需要实名认证**，且 Gitee Pages 已关闭。

| 需求 | 最佳方案 |
|------|---------|
| **不想实名认证，国内凑合用** | **GitHub Pages**（当前方案，国内多数地区可访问） |
| **愿意实名认证，求省事** | **Coding Pages**（腾讯云，稳定快） |
| **彻底离线/局域网** | 局域网方案（mkcert HTTPS + iPad 本地访问） |

### 10.5 局域网方案（备选）

如果不愿意实名认证和折腾备案，局域网方案是最省心的：

```bash
# 1. 构建
npm run build

# 2. 安装 mkcert 生成本地 HTTPS 证书
brew install mkcert
mkcert -install
mkcert 192.168.x.x   # 替换为你的局域网 IP

# 3. 启动 HTTPS 服务器
npx serve dist -l 3443 --ssl-cert ./192.168.x.x.pem --ssl-key ./192.168.x.x-key.pem

# 4. iPad 安装根证书，Safari 访问 https://192.168.x.x:3443
# 5. 添加到主屏幕（HTTPS 支持 PWA 全屏）
```

### 10.6 切换部署方式的注意事项

1. **数据不丢失**：数据存在 iPad 本地 IndexedDB，切换域名后数据仍在
2. **PWA 重新安装**：不同域名需要重新添加到主屏幕
3. **旧域名可以保留**：同时部署多个平台不影响数据

### 10.7 当前部署信息

| 项目 | 值 |
|------|-----|
| 访问地址 | `https://houcongcong86.github.io/daka-jingling/` |
| 部署方式 | GitHub Actions CI/CD，推送到 main 分支自动部署 |
| 仓库 | `https://github.com/houcongcong86/daka-jingling` |
| 构建产物 | `dist/` 目录（已提交到仓库） |
| 关键技术 | vite-plugin-pwa 生成 Service Worker，hash 路由 |

### 10.8 部署验证清单

| # | 检查项 | 预期结果 |
|---|--------|---------|
| 1 | 通过 URL 正常访问 | 打卡精灵首页显示 |
| 2 | 添加到主屏幕 | 桌面生成图标 |
| 3 | 全屏启动 | 无浏览器地址栏 |
| 4 | 麦克风权限弹窗 | 系统弹出权限请求 |
| 5 | 语音识别 | 说"今天有什么任务"能正常识别 |
| 6 | 语音合成 | 打卡精灵语音回复 |
| 7 | 手动点击任务 | 任务可标记完成 |
| 8 | 积分更新 | 完成任务后积分增加 |
| 9 | 进入家长模式 | 密码验证后可管理任务 |
| 10 | 断网打开（需先访问过） | 页面正常显示 |
